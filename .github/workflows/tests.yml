name: Automated Tests

on:
  pull_request:
    branches: [main]

jobs:
  vitest:
    name: Vitest Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Run tests
        run: bun run test
